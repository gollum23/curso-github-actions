name: reto-curso
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup
        uses: actions/setup-python@v3
        with:
          python-version: "3.10"
      - name: run-script
        run: python hello_world.py
